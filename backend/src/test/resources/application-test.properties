# Application name
spring.application.name=project-test

# Database connection settings
spring.datasource.url=jdbc:mysql://localhost:3306/tutor_managment_system?useSSL=false
spring.datasource.username=root
spring.datasource.password=z00i00a00
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.type=trace

# Server settings
server.port=8080

# Application name for test
spring.application.name=project-test

# Test database configuration (in-memory H2)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# JPA/Hibernate settings for test
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server settings (optional for tests, using a different port to avoid conflicts)
server.port=8091

# H2 Console (optional, for debugging during tests)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
